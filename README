A simple, modular, Perl-based IRC bot.

List of plugins:

 -- YouTube --
 Automatically gives info on YouTube links posted.

 -- GoogleImages --
 Responds to the command: !gis <query>
 Performs a Google ImageSearch for the given query and returns a random result.
